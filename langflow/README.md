# Langflow Notes

Langflow is not wired into this starter yet.

Use this folder later when you want to:

- visually map the recommendation pipeline
- route between support, styling, and image flows
- test prompt chains before moving them into code

For the MVP, start with direct FastAPI service functions first.
Only add Langflow after the basic flows already work.

